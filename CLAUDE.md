# CLAUDE.md — このリポジトリの憲法

このファイルは2つの役割を持つ。

1. Claude Code / Gemini などの AI がこのリポジトリを触るときの作業ルール
2. **投稿文を生成するときのプロンプトにそのまま埋め込まれる「発信ポリシー」**（`scripts/generate-week.mjs` と `scripts/generate-note.mjs` が §3〜§5 を読み込む）

つまり §3 以降を書き換えると、翌週から投稿の文体が変わる。ここが唯一の調整つまみである。

---

## §1 このシステムは何をするものか

GIGAyama（小学校教員）が公開している学習アプリ群を題材に、X と note の投稿を
「文章・画像・ハッシュタグ・URL がすべて用意された状態」まで自動で作る。

**人がやるのは投稿ボタンを押すことだけ。** 発信が続かない理由は意志の弱さではなく、
毎回「何を書くか考える → 文章を書く → 画像を用意する」という3つの作業が発生することにある。
そこを丸ごと機械に渡す。

## §2 技術的な前提（変更すると全体が壊れる）

- **X API は使わない。** 2026年2月に無料枠が廃止され、従量課金のみになった
  （URLなし $0.015 / URLあり $0.20 per post）。このシステムの目的の一つは費用を月0円に保つこと。
- **本文に URL を入れない。** X は本文に外部リンクがある投稿のリーチを大きく下げ、
  Premium でないアカウントではほとんど表示されない。リンクは自分への最初の返信に置く
  （`config/guardrails.json` の `urlPlacement`）。費用の面では本文に入れて困らないが、
  読まれないなら発信した意味がない。ランチャーが「①本文 → ②リンクの返信」の順に案内する。
  生成時（`lib/lint.mjs`）と配信物（`npm run check`）の両方で見ている。二重にする価値のある失敗である。
- 投稿は **Web Share API**（`navigator.share({files, text})`）でスマホの共有シートを開き、
  ユーザーが X アプリを選んで投稿ボタンを押す。画像添付と本文の事前入力はこれで両立する。
- X の Web Intent（`x.com/intent/post`）は本文を入れられるが **画像は添付できない**。
  PC・共有シート非対応環境向けのフォールバックとしてのみ使う。
- **添付する画像は選べる。** X は1投稿に4枚まで載る。候補は紹介カードと、
  アプリのリポジトリに置いてある画像（note の記事のために撮ったもの）。
  後者は **`raw.githubusercontent.com` から直接読む。このリポジトリに取り込まない**。
  1アプリで数MBあり、毎週コミットする以上そのままリポジトリの重さになるためである
  （KANJI_Town だけで28枚・約5MB）。代わりに圏外では添付できない。
  外を読む唯一の場所なので、行き先は3か所で押さえる。`docs/index.html` の CSP
  （`img-src` と `connect-src` の**両方**。片方だけだと画面には出るのに共有すると付かない）、
  `docs/lib/media-pick.js` の `isAllowedMediaSrc`、`npm run check` の FOREIGN_MEDIA。
  画像の置き場所は repo ごとに違うので決め打ちにしない（`docs/note/images/` も
  `docs/screenshot-*.png` もある）。「画像である」から始めてアイコンを落とす形にしてある
  （`scripts/lib/repo-images.mjs`）。URL は**コミット SHA で固定する**。
  ブランチ名で組むと、アプリ側で絵を差しかえたときに画面と共有物が食い違い、しかも気づけない。
- **note に公式 API は無い。** 非公式 API の利用は規約違反でアカウント停止の可能性があるため使わない。
  本文を生成してクリップボードに入れ、note のエディタを開くところまでを自動化する。
- 文章生成は **Gemini API の無料枠**。SDK は入れず `fetch` で直接叩く。
  **モデル名を直書きしない。** API に「いま使えるモデル」を聞いて、
  そのなかからいちばん新しい安定版を選ぶ（`scripts/lib/gemini-models.mjs`）。
  Google は数か月おきに新しい版を出し、古い版はいずれ止まる。
  名前を追いかけつづけるのは人の仕事として続かないし、止まった日に週次が丸ごと落ちる。
  既定では preview / exp を選ばない。予告なく変わるものに毎週の生成を預けないためである。
  選んだ結果は `data/gemini-model.json` に残してコミットする。
  いつどの版に切り替わったかが git の履歴で追えるようにするため。
- 時刻はすべて **JST**。GitHub Actions の cron は UTC なので、日付判定は必ず
  `scripts/lib/jst.mjs` を経由すること。素の `new Date()` で日付を出すと
  「日本時間の朝7時のはずが前日扱いになる」という事故が起きる。`npm run check` で検出する。
  ブラウザ側（`docs/`）は `docs/lib/jst-client.js` を通す。写しなので、
  `tests/docs-jst.test.mjs` が両者の結果が一致することを確かめている。
- **反応の記録は GitHub Issue で往復させる。** ランチャーが本文を載せた Issue 作成画面を開き、
  本人が送信ボタンを押す。週次が読んで `data/feedback.json` にまとめる。
  画面に書き込み権限のトークンを持たせない。集計値は足しこまず、投稿ごとの記録から毎回作りなおす
  （足しこむ形だと、同じ Issue を二度読んだだけで数がずれ、しかも誰も気づけない）。
- **画面から生成を発火させるのも、同じ Issue の道を通す。** 週次は日曜の夜にしか動かないが、
  「このアプリの話をいま出したい」は予定と関係なくやってくる。ブラウザから Gemini を呼べない以上、
  注文（`docs/lib/order.js`）を Issue にして送り、ワークフローが作ったものを受け取る形になる。
  返信の下書きとの違いは**結果の返し先**である。返信案は読んで書き直すものなのでコメントでよいが、
  投稿は共有シートに渡すものなので、画面に戻らないと意味がない。
  だから結果は同一オリジンの `docs/orders/<注文ID>.json` に置く。
  `api.github.com` を画面から読む形にはしない（外を読む先が増えるうえ、未認証は1時間60回で頭打ちになる）。
  **注文IDはそのままファイル名になる。** 外から来た文字列でパスを組む唯一の場所なので、
  形（`ORDER_ID_RE`）を決めて受け取る側で必ず照合する。緩めると Issue を立てるだけで
  リポジトリの任意の場所に書けてしまう。
- **`docs/` を push するワークフローを足したら、`deploy-pages.yml` の `workflow_run` に必ず名前を足す。**
  GITHUB_TOKEN による push は他のワークフローを起動しない。足し忘れると、
  ワークフローは緑、コミットも入っている、なのに画面だけ永久に古いまま、という
  どこにもエラーの出ない壊れ方をする。`npm run check` が両者のずれを検出する。
- **受け取った投稿は端末に写す。** `docs/orders/` は溜まりすぎないよう古いものから消える。
  ランチャーは一度受け取ったら `localStorage` に写し（`docs/lib/mine.js`）、以後ネットワークを見ない。
  ただし端末のなかにしか無いものになるので、書き出し／読みこみを必ず用意しておくこと。
- **枠（slot）は記録に書く。IDから読まない。** 反応の記録は投稿IDの後半を枠名として数えていたが、
  注文して作った投稿のIDは日付ごとに変わる。IDから読んだままだと `posted.bySlot` が
  一度きりのキーで埋まり、`config/slots.json` を見なおす材料が濁る。
- **`docs/sw.js` の `VERSION` は手で書かない。** `npm run build:sw` が `docs/` の中身から
  計算して書きこむ。直書きのままだと、画面を直しても端末に古い版が残りつづける。
  `npm run check` がずれを検出する。生成物（`docs/apps.html`）に「いま」を埋めないこと。
  実行のたびに変わると、入力から作りなおして一致するかを確かめられなくなる。

## §3 発信ポリシー（プロンプトに埋め込まれる）

**話者**: 現役の小学校教員。教室で実際に子どもを見ている人間。
**読者**: 同じ現場の先生、教育に関心のある保護者、GIGA 端末の運用担当者。
詳しい読者像は `config/audience.json`。「同じ学年を持っている、隣のクラスの先生ひとり」に
話しかけるつもりで書く。大勢への呼びかけにすると、誰の関心も引かない。

**最初の1行がすべて**。タイムラインで最初に見えるのはそこだけで、
そこで止まらなければ本文は読まれない。フックの型は `config/audience.json` の `hooks` にある。
ただし**煽って止めない**。静かでも、具体があって明日そのまま真似できるほうを選ぶ。
生成では1枠につき3案書かせ、読者側に立った「編集者」役が選ぶ。

**守ること**

- 一人称は「わたし」。教員どうしの職員室での会話くらいの距離感で書く。
- **具体を1つ入れる。** 「使いやすい」ではなく「45分の授業で、配って説明するのに2分で済む」のように書く。
- **困りごとから入る。** アプリの機能紹介から始めない。「〜で困っていた」→「だからこう作った」の順。
- 断定しすぎない。「効果が上がります」ではなく「〜だと思います」。
- 敬体（です・ます）で統一する。
- ハッシュタグは2〜3個まで。多いと宣伝色が強くなり読まれない。

**教室での出来事を、実際に見たこととして書かない。**
文章を作っているのは機械で、その日その教室で何が起きたかを知らない。
「うちのクラスでは子どもたちが喜んでいました」と書けば、
それは本人の名前で出る嘘になる。書いてよいのは次の形まで。

- 困りごとの描写 … 一般に起きることとして書く（「計算練習の時間、手が止まる子がいます」）
- 作った理由 … 「こうなればいいと思って作りました」
- 見立て … 「〜だと思います」「〜になればと考えています」

実際の授業でどうだったかは、投稿する前に本人が自分で足すところである
（ランチャーの［本文を直す］がそのためにある）。
`config/guardrails.json` の `experiencePatterns` が機械で見ている。

**絶対に書かないこと**

- 児童・生徒の名前、写真、発言の特定できる引用
- 勤務校名、自治体名、同僚の名前
- 学習効果の科学的根拠がない断定（「学力が上がる」「◯◯に効く」）
- 他の教材・他社サービスの批判
- 教育委員会・文科省の見解であるかのような書き方
- 絵文字の多用（1投稿につき2個まで。0個でもよい）
- 「バズ」を狙った煽り、過度な感嘆符、「知らないと損」系の表現

## §4 投稿の型

`config/themes.json` に定義がある。1週間のあいだ同じ型が続かないようローテーションする。
型ごとの狙いは themes.json の `intent` に書いてあるので、生成時はそれに従う。

**題材は「いま困っていること」で選ぶ。** 内容の良し悪しより、時期が合っているかで読まれ方が決まる。
所見に追われている12月に運動会の話をしても読まれない。
学校の1年は `config/calendar.json` に持たせてある（毎年ほぼ同じなので機械で持てる）。
その年その週にだけ起きていることは `scripts/research-trends.mjs` が検索で拾う。
拾った話題は**題材を選ぶための材料**であって、書く内容そのものではない。
ニュースの解説を書く場所ではないし、公的機関の見解のような書き方は §3 で禁じている。

週をまたいだ重複は `data/history.json`（`scripts/archive-history.mjs` が書く）で避ける。
履歴には**本文・最初の1行の型・落選案まで写す**。id と日付だけだったころは、
「先月と同じ言い回しか」を比べる相手がそもそも無く、出しなおす材料も
`data/queue/` にしか無かった（そのせいで `data/queue/` を消せず、ただ増えつづけていた）。
似ぐあいは文字2-gram の Jaccard 係数で測る（`scripts/lib/similar.mjs`）。
形態素解析は入れない。実行時の依存を増やさないという方針を、この程度のことで崩さない。

**似ているだけで投稿を捨てない。** 同じアプリの話は、書き方を変えても文字が似る。
捨てると正しい投稿まで消えて枠が空く。書きなおしのときに指摘するところまでにして、
捨てるかどうかを決めるのはガードレール（`lib/lint.mjs`）の仕事にしておく。

`data/feedback.json` に「反応がよかった」が溜まると、その型が選ばれやすくなる。
アプリごとの評価も効くが、型より弱くしてある。アプリは52件あるので、
1件の評価で順番が大きく動くと、同じアプリばかり出るようになるためである。
**最初の1行の型（hook）は重みにしない。** プロンプトに実績を材料として渡すだけにする。
フックを選ぶのは生成側の仕事で、機械が指定すると効いた型ばかりが並び、
「案どうしの切り口を変える」という指示と食い違う。

反応がよかった投稿は、`repriseAfterDays`（既定30日）を置いて出しなおす。
タイムラインは流れるので、1回出しただけではフォロワーの大半は見ていない。
**そのとき選ばれなかった案を本文にする。** 同じ文章を二度出すのではなく、
同じ題材を別の言い方で出しなおす形にすれば、生成を1回も呼ばずに1枠が埋まる。

**題材を人が選ぶ道も開けてある。** 上は「機械が選ぶ」側の話で、割り当ての偏りを避けるための規則である。
ただし、いま何を出したいかは本人がいちばん分かっている（誰かに聞かれた、その教科の研究授業が近い）。
ランチャーの［つくる］でアプリと型を指定して注文できる（`scripts/generate-promo.mjs`）。
**書き方の約束は共通にする。** 週次と注文で別々のプロンプトを持つと、
片方だけ直したときに「同じアカウントなのに、経路によって文体が違う」という壊れ方をする。
本文の書き方は `scripts/lib/draft.mjs` に、配る形は `scripts/lib/launcher-post.mjs` に1つずつ置く。
検査（`lib/lint.mjs`）は経路によらず同じものを通す。押した瞬間に出すものなので、あとから直す機会がない。

注文で「おまかせ」のときは `update`（アップデート報告）と `insight`（教育×AIの気づき）を既定の候補から外す。
前者は最近更新されたアプリにしか書けず、無いのに書かせると事実でない文章になる
（最近 push があるアプリのときだけ候補に戻す）。後者はアプリそのものを主題にしない型なので、
宣伝を頼まれている場に既定で混ぜるのは筋が違う。名指しで頼まれたときだけ使う。

## §5 収益化について

`config/monetization.json` の `enabled` が `false` のあいだは、
**収益化の導線を一切入れない**（アフィリエイトリンク、有料記事への誘導、支援リンク、
「詳しくは有料版で」といった表現をすべて禁止する）。

これは公立学校教員の兼業・副業が地方公務員法38条の許可制であり、
本人がまだ勤務先に確認していないためである。確認が済んで `enabled: true` にしたとき、
`monetization.json` の `links` と `ctaStyle` に従って導線を入れる。

## §6 コードを書くときのルール

- コメントは日本語で「なぜそうしたか」を書く。「何をしているか」はコードを読めば分かる。
- 実行時の依存パッケージを増やさない。`fetch` と Node 標準モジュールで足りるならそうする。
- 生成物（`data/` と `docs/media/`）はコミットする。GitHub Pages から配信しないと
  Web Share API に画像を渡せず、システムが成立しないためである。
- 秘密情報はすべて GitHub Secrets に置く。`data/` や `docs/` に書き出さない。
- 失敗したときは黙って続けない。どのアプリのどの工程で落ちたかを標準エラーに出して終了コードを返す。
  止めるか続けるかの判断はワークフロー側の仕事なので、スクリプトは必ず終了コードで伝える。
- **外から来る入力は、まず疑う。** このリポジトリは公開されていて、Issue は誰でも立てられる。
  反応の記録は投稿者を確かめ、中身の形も検査してから取り込む。
  1つでもおかしければ、その Issue はまるごと見送る（一部だけ採って残りを黙って捨てない）。
- **差分の判定にファイルの更新時刻（mtime）を使わない。** git は mtime を保存しないので、
  `actions/checkout` は全ファイルの mtime をチェックアウト時刻にする。
  つまり CI では常に「ファイルのほうが新しい」となり、判定が意味を失う。
  中身から計算した指紋を使うこと（`docs/media/manifest.json` がその例）。
- **検査は「0件でした」を信じない。** わざと壊して落ちることを確かめてから信じる。
  落ちないなら、それは合格しているのではなく何も見ていない。
