# 使い方

README.md が「何のための仕組みか」、こちらが「実際にどう使うか」です。

---

## §1 毎日やること（1〜2分）

1. 朝、スマホに GitHub の通知が届く（「今日の投稿 2 件」）
2. 通知を開いて **［投稿ランチャーをひらく］** を押す
3. カードの **［𝕏 に共有（画像つき）］** を押す
4. 共有シートで **X** を選ぶ
5. 画像と本文が入った投稿画面が開くので、**投稿ボタンを押す**
6. ランチャーに戻って **［次へ］** を押す
7. **［この文をコピーする］** を押し、X で**いま出した投稿に返信して貼り付ける**
8. ランチャーで **［投稿した］** を押す

### なぜ2回に分けるのか

X は、**本文に外部リンクがある投稿をほとんど表示しません。**
リンク付きの投稿はリーチが 30〜50% 落ち、Premium でないアカウントでは埋もれます。

そこで本文にはリンクを入れず、**リンクは自分への最初の返信に置いています。**
手数は1回増えますが、読まれる数が変わります。

カードの上に **①本文 → ②リンクの返信** と出ているので、順に押していけば終わります。
投稿が長くて［つづき］がある日は、そのぶんコマが増えます。

> 元に戻したいときは `config/guardrails.json` の `urlPlacement` を `"body"` にしてください。
> 本文に URL が入る形（1回で終わる形）に戻ります。

ホーム画面にランチャーを追加してあれば、通知を待たずにアイコンから開いても同じです。

### 共有シートに X が出ないとき

**［コピーして X を開く］** を使ってください。本文はクリップボードに入っていて、
X の投稿画面が開きます。画像を付けたいときは先に **［画像を保存］** を押しておき、
X の画面で写真を選んでください。

### 本文が入っていないとき

iOS では、画像と本文をいっしょに共有すると本文だけ落ちることがあります。
**［𝕏 に共有］を押した時点で本文はクリップボードにも入っている**ので、
投稿画面で長押し → ペーストしてください。

---

## §2 週に1回やること（5分）

### note を出す

ランチャーの **note** タブを開き、**［本文をコピーして note を開く］** を押します。

1. note のエディタが開く
2. タイトル欄に **［タイトルをコピー］** した文字列を貼る
3. 本文欄に貼り付ける
4. 見出しにしたい行を選んで、note の見出しボタンを押す
5. 公開

> 本文を Markdown ではなくプレーンテキストで出しているのは、
> note のエディタが `## 見出し` をそのまま文字として残してしまうためです。

### 反応を記録して送る

**投稿ずみ** タブを開き、伸びた投稿に **［反応よかった］**、
手応えがなかったものに **［いまいち］** を押します。

押しただけでは、この端末のなかに残るだけです。**タブの上に出る
［反応をまとめて送る（n件）］** を押してください。GitHub の Issue 作成画面が開くので、
中身を確かめて緑の **［Create］** を押します。1週間ぶん（14件）が1通で送れます。

> なぜ送信ボタンを押す必要があるのか
>
> 画面から GitHub に直接書き込むには、書き込み権限のあるトークンをスマホの
> ブラウザに置くことになります。この用途に対してそれは重すぎます。
> 本文を載せた Issue の作成画面を開くだけなら、トークンは要りません。
> 送信ボタンを押す1回だけが、人の手として残ります。

送られた記録は、日曜の週次ワークフローが読み取って `data/feedback.json` にまとめ、
Issue に「n件を取り込みました」と返事をして閉じます。

この記録は翌週の生成に効きます。「困りごと→解決」の型が伸びていれば、
その型が選ばれやすくなります。アプリごとの評価も少しだけ効きます
（アプリは52件あるので、1件の評価で偏りすぎないように型より弱くしてあります）。

**最初の1行の型（フック）も一緒に送っています。** タイムラインで最初に見えるのは1行目だけなので、
ここがいちばん効きます。「教室のある場面から入る」が伸びていれば、
翌週の生成にその実績が材料として渡ります。

**評価を付けなくても、［投稿した］だけで送れます。** どの枠なら実際に出せているかが分かると、
`config/slots.json`（朝と夜の2枠でよいのか）を見なおす材料になります。

**反応がよかった投稿は、30日以上あけて出しなおされます。** タイムラインは流れるので、
1回出しただけではほとんどの人が見ていません。そのとき選ばれなかった案を本文にするので、
同じ文章にはなりません。カードに［出しなおし］と出ます。
間隔は `config/themes.json` の `rotation.repriseAfterDays` で変えられます（0 で無効）。

**投稿ずみ** タブに数字のバッジが出ているあいだは、まだ送っていない記録があります。

### 予定に無い投稿を出す（いま出す）

**いま出す** タブに、その週の予定に出ていないアプリぶんの下書きが10本ほど入っています。
型で絞れるので、出したいものを選んでそのまま共有できます。

> なぜ作り置きなのか
>
> その場で文章を作るには、ブラウザから Gemini を呼ぶことになります。
> そのためには API キーを画面に持たせるしかなく、公開されるファイルに秘密情報は置けません。
> 日曜の夜にまとめて作っておけば、押した瞬間に出せて、費用も増えません。

### 返信の下書きを頼む

**いま出す** タブのいちばん上に、返信の下書きを頼む欄があります。
返したい相手の投稿を貼って **［下書きを頼む］** を押すと、GitHub の画面が開きます。
**［Create］** を押すと、数十秒後に立ち位置の違う返信案が3つコメントで届きます。

> X では、他の人の投稿への返信が交流として評価されます。
> 自分の投稿を並べるだけのアカウントは、どれだけ数を出しても伸びにくいところがあります。
>
> ただし返信は相手のある行為なので、**案を出すところまでで止めています。**
> そのまま使わず、自分の言葉に直してから返してください。
> 相手の投稿が批判や政治的な対立を含むときは、案を出さずに理由だけ返ってきます。

### 別の案に差し替える

生成のときに1枠あたり3案書かせて、読者側に立った「編集者」役が1つ選んでいます。
選ばれなかった案も残してあるので、**［別の案］** から読み比べて選び直せます。
選ぶ目は本人のほうが確かなので、機械の判断を最終決定にしていません。

### 本文を直したいとき

カードの **［本文を直す］** から、その場で書きかえられます。
文字数のカウントが連動するので、280 を超えていないか見ながら直せます。

直した内容はこの端末にだけ残ります（生成には戻しません。戻す経路を作ると
「AI が書いた文」と「人が直した文」の境目が消えて、次の生成の材料が濁るためです）。

---

## §3 自動で動いているもの

| いつ | 何が動くか | 中身 |
|---|---|---|
| 日曜 20:00 JST ごろ | 週次ワークフロー | 反応の取り込み → 履歴へ移す → 古い週の掃除 → 収集 → 理解 → 素材 → 生成 → 検査 → 配信 |
| 毎朝 6:30 JST ごろ | 通知ワークフロー | 朝の枠を Issue にする |
| 毎晩 20:00 JST ごろ | 通知ワークフロー | 夜の枠を Issue にする |

通知は `config/slots.json` の枠のぶんだけ立ちます。枠を増やしたときは
`.github/workflows/daily-notify.yml` の cron も増やしてください
（足し忘れは `npm run check` が知らせます）。

先頭の2つが「先週の結果を翌週の材料にする」工程です。

- **反応の取り込み** … `labels:feedback` の Issue を読んで `data/feedback.json` に反映し、
  取り込んだ Issue を閉じます。ここは失敗しても止まりません（反応が無くても下書きは作れる）。
- **履歴へ移す** … 終わった週を `data/history.json` に写します。ここは失敗すると止まります。
  落ちたまま進むと、週をまたいだ重複回避が黙って効かなくなり、
  「なぜか先週と同じアプリばかり出る」という気づきにくい壊れ方をするためです。
  本文・最初の1行・落選案まで写しているので、
  「先月と同じ言い回しになっていないか」の判定と、出しなおし（再放送）の材料になります。
- **古い週の掃除** … 26週より古い `data/queue/` `data/note/` `data/trends/` を消します。
  本文は履歴に残っているので、消しても上の2つは効きつづけます。

**週次が失敗したときは Issue が立ちます。** 同じ知らせが開いているあいだは重ねて立てないので、
直したら閉じてください。今週ぶんの下書きがまったく無い日は、
毎日の通知が予備の引き出しから2件を提案します。

GitHub Actions の定期実行は混雑時に数十分遅れます。
何時に投稿するかは人が決めるので、遅れても実害はありません。

手で動かしたいときは **Actions** タブ → ワークフローを選ぶ → **Run workflow** です。

---

## §4 投稿の内容を変えたい

### 誰に向けて書くかを変える

`config/audience.json` です。読者像がぼやけていると、当たりさわりのない文章になります。
当たりさわりの無い文章は、誰の関心も引きません。

- `primary.who` … いちばん届けたい相手
- `primary.wants` / `primary.hates` … その人が求めているもの・読むのをやめる理由
- `hooks` … 最初の1行の型。タイムラインで最初に見えるのはここだけです
- `avoid` … 使わない書き出し（「〜を作りました」など、宣伝だと分かって読むのをやめられるもの）

### 時期に合わせる

`config/calendar.json` に学校の1年が入っています。
所見の時期には評価と記録の話、行事の時期には短い時間で成立する話、というように題材が寄ります。

2学期制の学校や、行事の時期が違う場合はここを直してください。
**ここを直すと翌週から題材の選び方が変わります。**

その年その週にだけ起きていること（制度の変更、現場で回っている話題）は、
週次のときに検索で拾っています（`npm run trends`）。拾えなくても行事暦だけで動きます。

### 文体・話し方を変える

`CLAUDE.md` の **§3 発信ポリシー** を書き換えます。ここはそのまま生成プロンプトに入ります。

例：もっとくだけた感じにしたい
```
- 一人称は「わたし」。教員どうしの職員室での会話くらいの距離感で書く。
        ↓
- 一人称は「ぼく」。友だちに話しかけるような、やわらかい調子で書く。
```

### 投稿の本数・時間帯を変える

`config/slots.json` の `slots` を増やします。

```json
{ "id": "noon", "label": "昼", "hour": 12, "minute": 0, "_intent": "昼休みのスクロール時間帯" }
```

増やした分だけ翌週から生成されます。**ただしボタンを押す回数も増えます。**
まずは朝・夜の2本で1か月続けてから考えることをおすすめします。

### 切り口（型）を足す

`config/themes.json` の `themes` に足します。`intent` がそのまま生成の指示になります。

```json
{
  "id": "question",
  "label": "問いかけ",
  "weight": 1,
  "intent": "答えを出さず、読者に考えてもらう問いを投げる。自分の迷いも正直に書く。",
  "structure": "①最近考えていること ②迷っている点 ③みなさんはどうしていますか"
}
```

### 特定のアプリを題材にしない

`config/accounts.json` の `excludeRepos` に名前を足します。

---

## §5 うまくいかないとき

### ランチャーを開いても「下書きがまだありません」

週次ワークフローが一度も成功していない可能性があります。
**Actions → 週次 — 翌週の投稿を用意する** を開いて、赤くなっていないか確認してください。

よくある原因:

| 症状 | 原因 | 直しかた |
|---|---|---|
| `GEMINI_API_KEY が設定されていません` | Secret 未登録 | 下の手順で登録 |
| `Gemini API 429` が続く | 無料枠の1日上限 | 翌日に再実行。`--limit` で分けても可 |
| `Gemini API 400 (INVALID_ARGUMENT): API key not valid` | キーの値が違う | Secret を貼りなおす（下記） |
| `Gemini API 403 (PERMISSION_DENIED)` | API が有効でないプロジェクトのキー | AI Studio で作りなおす |
| `Gemini API 404 (NOT_FOUND): models/... is not found` | モデル名が違う／その版が止められた | `node scripts/check-gemini.mjs` で選びなおす（下記） |
| Pages が 404 | Pages 未有効 | Settings → Pages → Source を GitHub Actions に |

### Gemini API キーを登録する

1. <https://aistudio.google.com/apikey> でキーを作る（無料・カード登録も不要）
2. **Settings → Secrets and variables → Actions → New repository secret**
   - **Name**: `GEMINI_API_KEY`（1文字でも違うと空のまま渡ります。コピー推奨）
   - **Secret**: 1で取ったキー
3. **Actions →「週次 — 翌週の投稿を用意する」→ Run workflow**

#### キーを貼りなおすときの注意

Secret の値に**前後の空白や引用符（`"` や `'`）が混ざっている**と、
「登録はされているが値が正しくない」状態になります。
この場合エラーは `API key not valid` になります。キーの文字列だけを貼ってください。

> 週次ワークフローは、何か処理を始める前に2段階で確かめます。
>
> 1. **シークレットが空でないか**（通信なし・約5秒）
> 2. **本当に API を呼べるか**（1回だけ小さく投げる・約2秒）
>
> 2段目があるのは、「登録されているが値が正しくない」を1段目では捕まえられないためです。
> これが無いと、リポジトリの数だけ同じエラーが並び、本当の理由がその繰り返しに埋もれます。
>
> **この2つの確認ステップを消さないでください。** 実際にどちらの失敗も起きました。

### Gemini のモデルまわり

モデル名は `config/accounts.json` に固定していません。既定は `"auto"` で、
実行のたびに「いま使えるモデル」を API に聞いて**いちばん新しい安定版**を選びます。
新しい版が出たら自動でそちらに移り、古い版が止められて 404 になることも防げます。

```bash
node scripts/check-gemini.mjs           # いま何が選ばれるか（実際に1回投げて確かめる）
node scripts/check-gemini.mjs --models  # 選べるモデルを良い順に並べる
```

いま何が選ばれているかは `data/gemini-model.json` にあります。
これはコミットしているので、**いつどの版に切り替わったかが git の履歴で追えます。**
週次のログにも `モデル: gemini-2.5-flash — 自動選択` のように出ます。

| したいこと | どうするか |
|---|---|
| 版を固定する | `geminiModel` にモデル名を直接書く（例: `"gemini-2.5-flash"`） |
| 文章の質を上げる | `geminiModelPrefer` を `"pro"` に。無料枠の上限に当たりやすくなります |
| 新しい版をいち早く試す | `geminiAllowPreview` を `true` に |
| いったん元に戻す | `data/gemini-model.json` を消して `node scripts/check-gemini.mjs` |

> **preview を既定で使わない理由**
>
> preview や exp のついた版は、途中で挙動が変わったり、予告なく消えたりします。
> 毎週の生成をそこに預けると、こちらが何もしていないのに文章の調子が変わります。
> 新しさより、来週も同じように動くことを優先しています。
>
> `gemini-flash-latest` のような「いちばん新しい」を名乗る別名も、同じ理由で既定では使いません。
> 中身が preview を指していることがあるためです。

> **モデル一覧を取れなかったとき**
>
> `geminiModelFallback`（既定 `gemini-2.5-flash`）に落ちて、そのまま先へ進みます。
> 一覧が引けないことを理由に、その週の投稿を作れなくするのは本末転倒だからです。
> ただし黙っては通しません。ログに `⚠ Gemini のモデル一覧を取れませんでした` と出ます。

### 「GitHub Pages へ配信」が2秒で失敗する（ログも出ない）

ジョブがステップを1つも実行しないまま失敗し、ログを開いても何も無い場合、
原因はコードではなく**リポジトリの設定**です。

`environment: github-pages` は GitHub の予約環境で、**Pages が有効になっていないリポジトリでは使えません**。
使えないとジョブは開始前に弾かれるため、ログが1行も残りません。

確認するのはこの3か所です。上から順に見てください。

1. **Settings → Environments → `github-pages` → Deployment branches and tags**
   → **No restriction** にする（または `main` を追加する）

   実行ページの上に
   `Branch "main" is not allowed to deploy to github-pages due to environment protection rules.`
   と出ていたらこれです。**ブランチの名前を変えたことがあると、
   環境側に古い名前が残っていて、この状態になります。**

2. **Settings → Pages → Build and deployment → Source** を **GitHub Actions** にする
   （「Deploy from a branch」だと、このワークフローからは配信できません）

3. **Settings → Actions → General → Workflow permissions** を
   **Read and write permissions** にする

直したら Actions から「GitHub Pages へ配信」を **Re-run failed jobs** してください。

> ワークフローには `preflight` ジョブがあり、可能なら Pages を自動で有効にします。
> それでも駄目なときは、上の2つを日本語で案内して止まります。
> `preflight` に `environment:` を付けると同じ堂々めぐりに戻るので、付けないでください。

### 投稿が更新されない

`launcher.json` はキャッシュしない設計なので、通常は開けば新しい週が出ます
（もしここをキャッシュすると「新しい週の投稿が永久に出てこない」という
とても分かりにくい症状になります。`npm run check` が検出します）。

画面の見た目や動きが古いままのときは、Service Worker が古い版を持っています。
新しい版が配信されると自動で1回読み直すようにしてあるので、
ランチャーを開きなおせば入れかわります。

> 以前は「アイコンを消して追加しなおしてください」と書いていました。
> `docs/sw.js` の `VERSION` が `'v1'` の直書きで、画面を直しても版が変わらず、
> 端末に古い画面が残りつづけたためです。
> いまは `npm run build:sw` が `docs/` の中身から版を計算して書きこみます。
> 直せば必ず版が変わり、端末にも届きます。ずれたままコミットしようとすると
> `npm run check` が落とします。

### アプリ一覧ページ（apps.html）が古い

`npm run build:apps` で作りなおせます。中身は `data/repos.json` と
`data/profiles/` から決まるので、材料が古ければページも古いままです。
その場合は `npm run collect` からやりなおしてください。

### 反応を送ったのに翌週に反映されていない

**Actions →「週次 — 翌週の投稿を用意する」→ ⓪ 反応の記録を取り込む** のログを見てください。

| ログに出ていること | 意味 | 直しかた |
|---|---|---|
| `... さんが立てたものなので取り込みません` | 別のアカウントで Issue を立てた | 本人のアカウントで送りなおす。共同で使うなら `config/accounts.json` に `feedbackAuthors` を足す |
| `本文に機械向けのブロックが…ありません` | Issue の本文を手で編集して壊した | ランチャーから送りなおす |
| `取り込みずみ` と出る | すでに反映ずみ | 問題ありません |
| ステップ自体が赤い | 権限不足 | ワークフローの `permissions` に `issues: write` があるか確かめる |

送った Issue には、取り込めたときも取り込めなかったときも返事が付きます。
Issue が開いたまま残っていて返事も無いなら、まだ週次が走っていません。

### 生成された文章がおかしい

その週だけ作りなおせます。

```bash
npm run generate -- --week 2026-W34
npm run build:index
```

繰り返し同じ問題が出るなら `CLAUDE.md` の §3 か `config/guardrails.json` を直してください。

### 画像が真っ白／文字が □□□ になる

日本語フォントが入っていない環境で撮っています。
週次ワークフローは `fonts-noto-cjk` を入れてから撮ります。手元で撮るときは同じものを入れてください。

```bash
sudo apt-get install -y fonts-noto-cjk       # Ubuntu / Debian
brew install --cask font-noto-sans-cjk-jp    # macOS
```

> いまは `npm run media` が撮りはじめる前にフォントの有無を確かめて、
> 無ければ何も撮らずに止まります。52枚ぜんぶ □□□ で作ってしまってから気づく、
> ということは起きません。
>
> 撮ったあとの「ほぼ単色（真っ白）」も機械的に見ています（`scripts/lib/png.mjs`）。
> `npm run check` が `BLANK_CARD` として落とします。

### スクリーンショットが撮れているアプリを知りたい

`docs/media/manifest.json` に全アプリぶんの記録があります。

```bash
node -e "const m=require('./docs/media/manifest.json');
  console.log(Object.entries(m.apps).filter(([,v])=>!v.shot?.ok).map(([n,v])=>n+' — '+v.shot.reason).join('\n'))"
```

`npm run check` も、撮れていない件数と一覧を警告として出します。

### スクリーンショットが撮れないアプリがある

ログインが要るもの、Google アカウント連携が要るもの、GitHub Pages を持たないもの
（Chrome 拡張など）は撮れません。その場合も**文字だけの紹介カードは作られる**ので、
投稿はできます。

---

## §6 やらないと決めたこと

| やらないこと | 理由 |
|---|---|
| X API で自動投稿する | 2026年2月に無料枠が廃止。URL付き投稿は1件約32円かかる |
| note に自動投稿する | 公式 API が無く、非公式 API は規約違反でアカウント停止の可能性がある |
| 投稿の反応を自動で取得する | X API の read も課金対象。手で押す2つのボタンで代用している |
| 承認画面を別に作る | 承認する手間が増えると続かない。ランチャーで見て出すか出さないか決めれば足りる |
| 外部の予約投稿サービスを使う | 無料枠が狭く、結局ネタを自分で作ることになって元の問題に戻る |
| 短尺動画（mp4）を作る | Playwright は webm しか出せず、mp4 にするには ffmpeg が要る。§7 に詳しく書いた |
| GIF を自前で書き出す | LZW と減色で400行を超え、画質と容量が読めない。複数枚のカードで代用する |
| ランチャーにトークンを持たせる | 反応の記録は Issue の作成画面を開くだけで足りる。書き込み権限をスマホに置く理由が無い |
| 直した本文を生成に戻す | 「AI が書いた文」と「人が直した文」の境目が消えて、次の生成の材料が濁る |

---

## §7 この先やれること

- **収益化の導線**：`config/monetization.json` の `enabled` を `true` にすると、
  生成プロンプトが `links` を読んで自然な形で導線を入れます（週1本まで）。
  兼業の可否を勤務先に確認してからにしてください。
- **1投稿に複数枚の画像**：`config/media.json` の `carousel.enabled` を `true` にすると、
  アプリを操作する前後の2〜4コマを撮って、複数枚のカードを付けます（既定はオフ）。
  撮影に少し時間がかかり、`docs/media/` の枚数もコマ数ぶん増えます。
  毎週コミットするので、リポジトリの重さに直接効きます。まず1枚で1か月続けてから考えてください。
- **反応の自動集計**：いまは「よかった／いまいち」を手で押しています。
  毎朝の通知 Issue に投稿ごとのコメントを付け、そこへの 👍 / 👎 を読む形にすれば、
  ランチャーを開かずに GitHub アプリだけで済みます。`data/feedback.json` を共有する設計なので、
  あとから足せます。
- **アプリ一覧ページ（apps.html）の強化**：投稿の着地先なのに OGP がないので、
  共有されてもカードが出ません。52件を縦に読ませる形なので、学年やキーワードでの
  絞り込みも要ります（`script-src 'none'` を `'self'` にして、同梱の小さな JS を足す形）。
- **書いた数字の裏取り**：「配って説明するまで2分」のような具体は、
  プロフィールに根拠が無くても書けてしまいます。いまは生成プロンプトの指示だけで、
  検査がありません。生成後にもう1回だけ編集者役に確かめさせれば塞げます。
- **設定が効いていないもの**：`config/monetization.json` の `ctaStyle` と
  `maxPromoPostsPerWeek`、`config/slots.json` の `noteCadence` と `noteDay`、
  各枠の `_intent`（朝は短く／夜は語ってよい）は、まだコードから読まれていません。
  `enabled: true` にした日に「書いたとおりに動かない」となるので、先に直すほうが安全です。

### 短尺動画をやらないと決めた理由

Playwright には録画機能がありますが、**出せるのは webm だけ**で、X は webm を受け付けません。
mp4 にするには ffmpeg が要り、実行時の依存を増やさないという方針（CLAUDE.md §6）を崩します。

自前で書き出す道も検討しました。

- **APNG** … zlib だけで組めます（IDAT を fdAT に詰めかえるだけ）。
  ただし **X はアニメーション APNG を静止画に潰します**。X 向けには意味がありません。
- **GIF** … X は受け付けます（サーバ側で mp4 にしてくれる）。
  ただし LZW エンコーダと減色の実装で 400 行を超え、画質と容量の当たり外れが大きい。
  かけた手間に対して見返りが読めません。

代わりに **連続カード画像**（上の `carousel`）にしました。既存のカード生成を繰り返すだけで、
確実に動きます。
