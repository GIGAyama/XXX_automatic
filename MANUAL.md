# 使い方

README.md が「何のための仕組みか」、こちらが「実際にどう使うか」です。

---

## §1 毎日やること（1分）

1. 朝、スマホに GitHub の通知が届く（「今日の投稿 2 件」）
2. 通知を開いて **［投稿ランチャーをひらく］** を押す
3. カードの **［𝕏 に共有（画像つき）］** を押す
4. 共有シートで **X** を選ぶ
5. 画像と本文が入った投稿画面が開くので、**投稿ボタンを押す**
6. ランチャーに戻って **［投稿した］** を押す

ホーム画面にランチャーを追加してあれば、通知を待たずにアイコンから開いても同じです。

### 共有シートに X が出ないとき

**［コピーして X を開く］** を使ってください。本文はクリップボードに入っていて、
X の投稿画面が開きます。画像を付けたいときは先に **［画像を保存］** を押しておき、
X の画面で写真を選んでください。

### 本文が入っていないとき

iOS では、画像と本文をいっしょに共有すると本文だけ落ちることがあります。
**［𝕏 に共有］を押した時点で本文はクリップボードにも入っている**ので、
投稿画面で長押し → ペーストしてください。

---

## §2 週に1回やること（5分）

### note を出す

ランチャーの **note** タブを開き、**［本文をコピーして note を開く］** を押します。

1. note のエディタが開く
2. タイトル欄に **［タイトルをコピー］** した文字列を貼る
3. 本文欄に貼り付ける
4. 見出しにしたい行を選んで、note の見出しボタンを押す
5. 公開

> 本文を Markdown ではなくプレーンテキストで出しているのは、
> note のエディタが `## 見出し` をそのまま文字として残してしまうためです。

### 反応を記録する

**投稿ずみ** タブを開き、伸びた投稿に **［反応よかった］**、
手応えがなかったものに **［いまいち］** を押します。

この記録は翌週の生成に効きます。「困りごと→解決」の型が伸びていれば、
その型が選ばれやすくなります。

---

## §3 自動で動いているもの

| いつ | 何が動くか | 中身 |
|---|---|---|
| 日曜 20:00 JST ごろ | 週次ワークフロー | 収集 → 理解 → 素材 → 生成 → 検査 → 配信 |
| 毎朝 6:30 JST ごろ | 通知ワークフロー | その日のぶんを Issue にする |

GitHub Actions の定期実行は混雑時に数十分遅れます。
何時に投稿するかは人が決めるので、遅れても実害はありません。

手で動かしたいときは **Actions** タブ → ワークフローを選ぶ → **Run workflow** です。

---

## §4 投稿の内容を変えたい

### 文体・話し方を変える

`CLAUDE.md` の **§3 発信ポリシー** を書き換えます。ここはそのまま生成プロンプトに入ります。

例：もっとくだけた感じにしたい
```
- 一人称は「わたし」。教員どうしの職員室での会話くらいの距離感で書く。
        ↓
- 一人称は「ぼく」。友だちに話しかけるような、やわらかい調子で書く。
```

### 投稿の本数・時間帯を変える

`config/slots.json` の `slots` を増やします。

```json
{ "id": "noon", "label": "昼", "hour": 12, "minute": 0, "_intent": "昼休みのスクロール時間帯" }
```

増やした分だけ翌週から生成されます。**ただしボタンを押す回数も増えます。**
まずは朝・夜の2本で1か月続けてから考えることをおすすめします。

### 切り口（型）を足す

`config/themes.json` の `themes` に足します。`intent` がそのまま生成の指示になります。

```json
{
  "id": "question",
  "label": "問いかけ",
  "weight": 1,
  "intent": "答えを出さず、読者に考えてもらう問いを投げる。自分の迷いも正直に書く。",
  "structure": "①最近考えていること ②迷っている点 ③みなさんはどうしていますか"
}
```

### 特定のアプリを題材にしない

`config/accounts.json` の `excludeRepos` に名前を足します。

---

## §5 うまくいかないとき

### ランチャーを開いても「下書きがまだありません」

週次ワークフローが一度も成功していない可能性があります。
**Actions → 週次 — 翌週の投稿を用意する** を開いて、赤くなっていないか確認してください。

よくある原因:

| 症状 | 原因 | 直しかた |
|---|---|---|
| `GEMINI_API_KEY が設定されていません` | Secret 未登録 | Settings → Secrets and variables → Actions に登録 |
| `Gemini API 429` が続く | 無料枠の1日上限 | 翌日に再実行。`--limit` で分けても可 |
| Pages が 404 | Pages 未有効 | Settings → Pages → Source を GitHub Actions に |

### 投稿が更新されない

Service Worker が古い画面を返している可能性があります。
ランチャーを開いたまま画面を下に引っぱって再読み込みするか、
一度ホーム画面のアイコンを消して追加しなおしてください。

> `launcher.json` はキャッシュしない設計になっています（`docs/sw.js`）。
> もしここをキャッシュすると「新しい週の投稿が永久に出てこない」という
> とても分かりにくい症状になります。`npm run check` が検出します。

### 生成された文章がおかしい

その週だけ作りなおせます。

```bash
npm run generate -- --week 2026-W34
npm run build:index
```

繰り返し同じ問題が出るなら `CLAUDE.md` の §3 か `config/guardrails.json` を直してください。

### 画像が真っ白／文字が □□□ になる

日本語フォントが入っていない環境で撮っています。
週次ワークフローは `fonts-noto-cjk` を入れてから撮ります。手元で撮るときは同じものを入れてください。

### スクリーンショットが撮れないアプリがある

ログインが要るもの、Google アカウント連携が要るもの、GitHub Pages を持たないもの
（Chrome 拡張など）は撮れません。その場合も**文字だけの紹介カードは作られる**ので、
投稿はできます。

---

## §6 やらないと決めたこと

| やらないこと | 理由 |
|---|---|
| X API で自動投稿する | 2026年2月に無料枠が廃止。URL付き投稿は1件約32円かかる |
| note に自動投稿する | 公式 API が無く、非公式 API は規約違反でアカウント停止の可能性がある |
| 投稿の反応を自動で取得する | X API の read も課金対象。手で押す2つのボタンで代用している |
| 承認画面を別に作る | 承認する手間が増えると続かない。ランチャーで見て出すか出さないか決めれば足りる |
| 外部の予約投稿サービスを使う | 無料枠が狭く、結局ネタを自分で作ることになって元の問題に戻る |

---

## §7 この先やれること

- **短尺動画**：Playwright にはブラウザ操作を録画する機能があります。
  アプリを触っている様子を10秒ほど撮って mp4 にすれば、X ではさらに伸びやすくなります。
- **収益化の導線**：`config/monetization.json` の `enabled` を `true` にすると、
  生成プロンプトが `links` を読んで自然な形で導線を入れます（週1本まで）。
  兼業の可否を勤務先に確認してからにしてください。
- **アプリ一覧ページ**：現在どのリポジトリにも「自分のアプリ一覧」がありません。
  `data/repos.json` から一覧ページを作れば、投稿の着地先として使えます。
